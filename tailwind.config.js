const { colors } = require('tailwindcss/defaultTheme');

module.exports = {
  theme: {
    colors: {
      primary: colors.purple,
      secondary: colors.purple,
      neutral: colors.gray,
      white: '#FFF',
    },
    extend: {},
  },
  variants: {},
  plugins: [],
};
